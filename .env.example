# Required
ANTHROPIC_API_KEY=

# Optional — logging (not wired in config.py yet)
LOG_LEVEL=INFO

# Optional — Langfuse observability (tracing disabled if unset)
LANGFUSE_SECRET_KEY=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_BASE_URL=https://cloud.langfuse.com
